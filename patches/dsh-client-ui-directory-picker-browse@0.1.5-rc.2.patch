diff --git a/lib/client.js b/lib/client.js
index 741dd6d..08d1ddd 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -280,7 +280,7 @@ window.__ModuleLoader__.load({
 		* @param props - owner-controlled browser props.
 		* @returns the dialog element (null while closed, via Modal).
 		*/
-		function DirectoryBrowser({ open, listDirectory, createDirectory, onOpen, onClose, busy, t }) {
+		function DirectoryBrowser({ open, listDirectory, createDirectory, pickNativeDirectory, validateDirectory, onOpen, onClose, busy, t }) {
 			const [parent, setParent] = (0, react.useState)(null);
 			const [selected, setSelected] = (0, react.useState)(null);
 			const [child, setChild] = (0, react.useState)(null);
@@ -293,6 +293,8 @@ window.__ModuleLoader__.load({
 			const [folderDraft, setFolderDraft] = (0, react.useState)(null);
 			const [creatingFolder, setCreatingFolder] = (0, react.useState)(false);
 			const [createError, setCreateError] = (0, react.useState)(null);
+			const [nativePicking, setNativePicking] = (0, react.useState)(false);
+			const [validatingDirectory, setValidatingDirectory] = (0, react.useState)(false);
 			const requestSeq = (0, react.useRef)(0);
 			const scanController = (0, react.useRef)(null);
 			const openGeneration = (0, react.useRef)(0);
@@ -670,12 +672,39 @@ window.__ModuleLoader__.load({
 			});
 			if (!open) return null;
 			const twoPane = selected !== null;
-			const parentInert = busy || folderDraft !== null;
+			const parentInert = busy || folderDraft !== null || nativePicking || validatingDirectory;
+			const openDirectory = (path) => {
+				if (validateDirectory === void 0) {
+					onOpen(path);
+					return;
+				}
+				setError(null);
+				setValidatingDirectory(true);
+				Promise.resolve().then(() => validateDirectory(path)).then((allowed) => {
+					setValidatingDirectory(false);
+					if (allowed) onOpen(path);
+				}, (reason) => {
+					setValidatingDirectory(false);
+					setError(failureText(reason));
+				});
+			};
+			const pickFromSystem = () => {
+				if (pickNativeDirectory === void 0) return;
+				setError(null);
+				setNativePicking(true);
+				Promise.resolve().then(() => pickNativeDirectory()).then((path) => {
+					setNativePicking(false);
+					if (path !== null) openDirectory(path);
+				}, (reason) => {
+					setNativePicking(false);
+					setError(failureText(reason));
+				});
+			};
 			const draftPending = pathDraft !== null;
 			return (0, react_jsx_runtime.jsxs)(_deepseek_ai_dsh_client_ui_primitives.Modal, {
 				open,
 				onClose: () => {
-					if (folderDraft === null && !busy) onClose();
+					if (!parentInert) onClose();
 				},
 				title: t("browser.title"),
 				className: clsx(DirectoryBrowser_module_css_default.dialog),
@@ -833,6 +862,16 @@ window.__ModuleLoader__.load({
 									},
 									children: t("browser.newFolder")
 								}),
+								pickNativeDirectory !== void 0 && (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.Button, {
+									type: "button",
+									variant: "outline",
+									size: "sm",
+									icon: (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconFolderOpen16, { size: 16 }),
+									"aria-busy": nativePicking || void 0,
+									disabled: parentInert,
+									onClick: pickFromSystem,
+									children: t("browser.nativePicker")
+								}),
 								(0, react_jsx_runtime.jsxs)("button", {
 									type: "button",
 									className: clsx(DirectoryBrowser_module_css_default.showHiddenToggle, showHidden && DirectoryBrowser_module_css_default.showHiddenToggleActive),
@@ -860,7 +899,7 @@ window.__ModuleLoader__.load({
 									disabled: targetPath === null || loading || parentInert || draftPending,
 									/* v8 ignore next -- narrowing guard: Open disables while no target exists. */
 									onClick: () => {
-										if (targetPath !== null) onOpen(targetPath);
+										if (targetPath !== null) openDirectory(targetPath);
 									},
 									children: t("browser.open")
 								})
@@ -956,6 +995,8 @@ window.__ModuleLoader__.load({
 				busy: props.busy,
 				listDirectory: props.listDirectory,
 				createDirectory: props.createDirectory,
+				pickNativeDirectory: props.pickNativeDirectory,
+				validateDirectory: props.validateDirectory,
 				t: props.t,
 				onOpen: props.onPicked,
 				onClose: props.onCancel
@@ -993,7 +1034,8 @@ window.__ModuleLoader__.load({
 					"browser.editPath": "编辑路径",
 					"browser.loading": "加载中…",
 					"browser.truncated": "文件夹过多，仅显示开头部分。",
-					"browser.showHidden": "显示隐藏文件"
+					"browser.showHidden": "显示隐藏文件",
+					"browser.nativePicker": "使用 Windows 选择文件夹"
 				}], ["en", {
 					"browser.title": "Select Workspace Directory",
 					"browser.home": "Home",
@@ -1007,7 +1049,8 @@ window.__ModuleLoader__.load({
 					"browser.editPath": "Edit path",
 					"browser.loading": "Loading…",
 					"browser.truncated": "Too many folders to list; only the beginning is shown.",
-					"browser.showHidden": "Show hidden files"
+					"browser.showHidden": "Show hidden files",
+					"browser.nativePicker": "Choose with Windows"
 				}]];
 				try {
 					for (const [locale, dict] of dictionaries) disposers.push(ctx.locale.register(LOCALE_NS, locale, dict));
@@ -1022,6 +1065,16 @@ window.__ModuleLoader__.load({
 			const injected = () => ({
 				listDirectory: (path, signal) => ctx.uiWorkspace.listDirectory(path, signal),
 				createDirectory: (path, name) => ctx.uiWorkspace.createDirectory(path, name),
+				pickNativeDirectory: new URLSearchParams(window.location.search).get("dsh-desktop-platform") === "win32" ? () => {
+					const pick = window.__DSH_DESKTOP_PICK_DIRECTORY__;
+					if (typeof pick !== "function") throw new Error("DSH Desktop native directory picker is unavailable");
+					return pick();
+				} : void 0,
+				validateDirectory: new URLSearchParams(window.location.search).get("dsh-desktop-platform") === "win32" ? (path) => {
+					const validate = window.__DSH_DESKTOP_VALIDATE_DIRECTORY__;
+					if (typeof validate !== "function") throw new Error("DSH Desktop directory validation is unavailable");
+					return validate(path);
+				} : void 0,
 				t: ctx.locale.bind(LOCALE_NS)
 			});
 			ctx.slots.inject("conversation.hero.workspace.directoryFlow", () => ctx.slots.inject("sidebar.workspaces.directoryFlow", function* () {
diff --git a/lib/types/client/DirectoryBrowser.d.ts b/lib/types/client/DirectoryBrowser.d.ts
index 2992216..8ff178a 100644
--- a/lib/types/client/DirectoryBrowser.d.ts
+++ b/lib/types/client/DirectoryBrowser.d.ts
@@ -17,6 +17,10 @@ export interface DirectoryBrowserProps {
      * business message over the ordinary Error text.
      */
     createDirectory: (path: string, name: string) => Promise<string>;
+    /** Optional native folder chooser contributed by a local desktop shell. */
+    pickNativeDirectory?: () => Promise<string | null>;
+    /** Optional owner validation before a selected path is opened. */
+    validateDirectory?: (path: string) => Promise<boolean>;
     /** The operator confirmed a directory (the selection, else the listed level). */
     onOpen: (path: string) => void;
     /** Close without picking (mask, Escape, Cancel). */
@@ -31,5 +35,5 @@ export interface DirectoryBrowserProps {
  * @param props - owner-controlled browser props.
  * @returns the dialog element (null while closed, via Modal).
  */
-export declare function DirectoryBrowser({ open, listDirectory, createDirectory, onOpen, onClose, busy, t }: DirectoryBrowserProps): import("react").JSX.Element | null;
-//# sourceMappingURL=DirectoryBrowser.d.ts.map
\ No newline at end of file
+export declare function DirectoryBrowser({ open, listDirectory, createDirectory, pickNativeDirectory, validateDirectory, onOpen, onClose, busy, t }: DirectoryBrowserProps): import("react").JSX.Element | null;
+//# sourceMappingURL=DirectoryBrowser.d.ts.map
diff --git a/lib/types/client/flow.d.ts b/lib/types/client/flow.d.ts
index e4e84ac..f4ee479 100644
--- a/lib/types/client/flow.d.ts
+++ b/lib/types/client/flow.d.ts
@@ -8,6 +8,10 @@ export interface BrowseFlowInjected {
     listDirectory: (path?: string, signal?: AbortSignal) => Promise<DirectoryListing>;
     /** Create one child directory under an existing parent. */
     createDirectory: (path: string, name: string) => Promise<string>;
+    /** Optional native folder chooser contributed by a local desktop shell. */
+    pickNativeDirectory?: () => Promise<string | null>;
+    /** Optional owner validation before a selected path is opened. */
+    validateDirectory?: (path: string) => Promise<boolean>;
     /** Localized dialog copy (this package's namespace). */
     t: Translate;
 }
@@ -21,4 +25,4 @@ export interface BrowseFlowInjected {
  * @returns the dialog element (renders nothing while closed).
  */
 export declare function BrowseDirectoryFlow(props: DirectoryFlowOwnerProps & BrowseFlowInjected): ReactElement;
-//# sourceMappingURL=flow.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=flow.d.ts.map
