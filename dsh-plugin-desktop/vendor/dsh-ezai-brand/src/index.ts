/** Host half of the EZAI brand plugin: an empty Loader seat. */

/**
 * Host plugin body — this package contributes browser presentation only; the
 * brand occupants ship through `exports["./client"]` and install into the
 * sidebar and conversation hero slots in the browser roster.
 */
export function apply(): void {}
